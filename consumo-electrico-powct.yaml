substitutions:
  # Para probar sin romper nada en Home Assistant, empieza con un nombre nuevo.
  # Cuando quieras sustituir el dispositivo antiguo manteniendo entity_ids,
  # cambia esto a `consumo-electrico` y flashea el POWCT con ese nombre.
  device_name: consumo-electrico-powct

packages:
  device_base: !include base_devices/sonoff-powct.yaml

esphome:
  friendly_name: Consumo electrico (POWCT)
  area: pasillo
