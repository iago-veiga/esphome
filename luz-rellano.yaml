substitutions:
  device_name: luz-rellano

packages:
  device_base: !include base_devices/sonoffmini.yaml
  behavior: !include common/behaviors/binary_light.yaml

esphome:
  friendly_name: Luz rellano
  comment: Sonoff Mini que controla la iluminación del rellano.
  area: rellano
