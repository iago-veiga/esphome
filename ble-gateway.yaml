substitutions:
  device_name: ble-gateway

packages:
  device_base: !include base_devices/esp32-wroom-32d.yaml

esphome:
  friendly_name: Gateway BLE
  comment: ESP32 dedicado como proxy Bluetooth para Home Assistant.
  area: pasillo

bluetooth_proxy:
  active: True
